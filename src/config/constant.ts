export enum SortType {
    GOLD = "gold",
    SILVER = "silver",
    BRONZE = "bronze",
    TOTAL = "total"
}

export enum StatusCode {
    OK = 200,
    NOT_FOUND = 404,
    INTERNAL_SERVER_ERROR = 500
}